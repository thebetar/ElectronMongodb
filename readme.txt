Steps to start:
1. Install mongod from commandline (and all other necessities)
2. Create DB dir in root
3. run "mongod --dbpath db --port 2400"
4. run "yarn install" / "npm install"
5. run "yarn start" / "npm start" 